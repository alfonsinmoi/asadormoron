<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="AsadorMoron.Views.Clientes.FranjasHorariasView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    BackgroundColor="{StaticResource Rojo}">
    <ContentPage.Resources>
        <Style TargetType="Grid">
            <Setter Property="VisualStateManager.VisualStateGroups">
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal" />
                        <VisualState x:Name="Selected">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor" Value="Transparent" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter>
        </Style>
    </ContentPage.Resources>
    <NavigationPage.TitleView>
        <Grid HorizontalOptions="FillAndExpand" ColumnDefinitions="*">
            <Label
                Grid.Column="0"
                Margin="0,0,50,0"
                FontFamily="Syne"
                FontSize="25"
                HorizontalTextAlignment="Center"
                Text="Hora "
                TextColor="{DynamicResource Amarillo}"
                VerticalOptions="Center" />
        </Grid>
    </NavigationPage.TitleView>
    <ContentPage.Content>
        <Grid BackgroundColor="White"
            Margin="{OnPlatform iOS='0,90,0,0',
                                Android='0,50,0,0',
                                UWP='0'}"
            RowDefinitions="Auto,*,Auto"
            VerticalOptions="FillAndExpand">
            <Label
                Grid.Row="0"
                Margin="0,20,0,0"
                HorizontalTextAlignment="Center">
                <Label.FormattedText>
                    <FormattedString>
                        <Span
                            BackgroundColor="Transparent"
                            FontFamily="Syne"
                            FontSize="22"
                            Text="{Binding Texto}"
                            TextColor="{DynamicResource Negro}" />
                    </FormattedString>
                </Label.FormattedText>
            </Label>
            <CollectionView
                Grid.Row="1"
                Margin="15,10"
                BackgroundColor="Transparent"
                ItemsSource="{Binding Listado}"
                SelectedItem="{Binding FranjaSeleccionada}"
                SelectionMode="Single">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="{OnIdiom Phone=2, Tablet=4, Desktop=4, Default=2}" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout BackgroundColor="Transparent">
                            <Frame
                                Margin="0,15"
                                Padding="40,15"
                                BackgroundColor="{Binding Color}"
                                CornerRadius="20"
                                HorizontalOptions="CenterAndExpand">
                                <Label
                                    FontAttributes="Bold"
                                    FontFamily="Syne"
                                    FontSize="22"
                                    HorizontalTextAlignment="Center"
                                    Text="{Binding horaInicio}"
                                    TextColor="{DynamicResource Blanco}">
                                    <Label.Triggers>
                                        <DataTrigger
                                            Binding="{Binding Color}"
                                            TargetType="Label"
                                            Value="#f0dd49">
                                            <Setter Property="TextColor" Value="{DynamicResource Negro}" />
                                        </DataTrigger>
                                    </Label.Triggers>
                                </Label>
                            </Frame>
                            
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup Name="CommonStates">
                        <VisualState Name="Normal" />
                        <VisualState Name="Selected">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor" Value="Transparent" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>
            </CollectionView>
            <Button
                Grid.Row="2"
                Margin="10,0,10,20"
                BackgroundColor="{DynamicResource Amarillo}"
                Command="{Binding EnviarPedidoCommand}"
                CornerRadius="10"
                FontFamily="Nunito"
                FontSize="18"
                HeightRequest="60"
                HorizontalOptions="FillAndExpand"
                Text="Enviar Pedido"
                TextColor="{DynamicResource Negro}"
                VerticalOptions="EndAndExpand" />
        </Grid>
    </ContentPage.Content>
</ContentPage>
